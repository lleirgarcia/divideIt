# Variables para Docker interno (docker-compose.internal.yml)
# Copia a .env en la raíz. No subas .env a git.

# Acceso por nombre (ej. http://divideit.local sin puerto):
# 1. Añade en /etc/hosts:  127.0.0.1  divideit.local
# 2. En .env:  DIVIDEIT_PUBLIC_URL=http://divideit.local
# 3. Reconstruye:  docker compose -f docker-compose.internal.yml build frontend && docker compose -f docker-compose.internal.yml up -d
# 4. Abre:  http://divideit.local  (nginx en puerto 80; NGINX_PORT=8080 si 80 ocupado)
# DIVIDEIT_PUBLIC_URL=http://divideit.local
# Si usas Google Drive con nombre, añade: GOOGLE_DRIVE_REDIRECT_URI=http://divideit.local/api/google-drive/oauth/callback

# Puertos directos (si 18080/18081 están ocupados por otros Docker)
# BACKEND_PORT=28081
# FRONTEND_PORT=28080

# Google Drive (opcional)
GOOGLE_DRIVE_CLIENT_ID=tu_client_id.apps.googleusercontent.com
GOOGLE_DRIVE_CLIENT_SECRET=tu_client_secret
GOOGLE_DRIVE_REDIRECT_URI=http://127.0.0.1:18081/api/google-drive/oauth/callback
GOOGLE_DRIVE_REFRESH_TOKEN=tu_refresh_token_obtenido_del_oauth
